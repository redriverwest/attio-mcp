ATTIO_API_KEY=your_attio_api_key
# Optional: bearer token if you want to require authentication
MCP_BEARER_TOKEN=
# Transport options: stdio (default), sse, websocket
MCP_TRANSPORT=sse
MCP_HOST=0.0.0.0
MCP_PORT=8080
LOG_LEVEL=INFO

