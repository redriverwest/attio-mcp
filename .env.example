# Attio API Configuration
# Get your API key from: https://app.attio.com/settings/api
ATTIO_API_KEY=your_attio_api_key_here

# MCP Server Authentication (OPTIONAL)
# If not set, the server will run without authentication (useful for development)
# Generate a secure token with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# MCP_BEARER_TOKEN=your_secure_bearer_token_here

# Optional: Server Configuration
LOG_LEVEL=INFO
